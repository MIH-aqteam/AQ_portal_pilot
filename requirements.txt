# Runtime for the scraper and the Sphinx build.
beautifulsoup4>=4.12
lxml>=5.0
markdownify>=0.13
sphinx>=7.2
myst-parser>=2.0
linkify-it-py>=2.0
furo>=2024.1.29

# Only needed to re-capture thumbnails of the map viewers (scripts/capture_cdp.py).
websocket-client>=1.7
